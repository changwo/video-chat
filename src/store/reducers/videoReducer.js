

const initialState = {
    data: null,
};

export const videoReducer = (state = initialState, action) => {
    const newState = {...state};
    switch (action.type) {
        default:
            return state;
    }
};
